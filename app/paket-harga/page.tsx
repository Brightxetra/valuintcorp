import { MarketingPage, metadataFor } from "../marketing-page";

export const metadata = metadataFor("paketHarga");

export default function PaketHargaPage() {
  return <MarketingPage pageKey="paketHarga" />;
}
